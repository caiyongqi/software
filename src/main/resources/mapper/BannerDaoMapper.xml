<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="center.cyq.software.dao.BannerDao">
    <select id="getBanners" resultType="Banner" parameterType="Integer">
        SELECT * FROM banner LIMIT #{nums}
    </select>
</mapper>